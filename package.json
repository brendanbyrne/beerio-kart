{
  "name": "beerio-kart",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && cargo run\" \"cd frontend && bun run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
